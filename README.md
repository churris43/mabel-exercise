# mabel

Usage:

docker compose run --rm app pytest — run the test suite
docker compose run --rm app python -m <your_module> — run the CLI once you build it
docker compose run --rm app bash — drop into a shell in the environment
