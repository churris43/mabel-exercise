[pytest]
# Add the project root to sys.path so tests can `import domain`, `import main`,
# etc. regardless of where the test file lives.
pythonpath = src/
testpaths = **/tests
addopts = -ra --import-mode=importlib
